/* 
 * Contact: team@fusioncode.ai
 *
 *
 * Project  - movies UNIT TESTS
 * Application Client
 * fanyc
*/

/* the movies view has a notification icon located at the bottom center of the view which on click will trigger the notification() action */
it("the movies view has a notification icon located at the bottom center of the view which on click will trigger the notification() action", () => {
  /* ToDo developer needs to complete unit test 
               the movies view has a notification icon located at the bottom center of the view which on click will trigger the notification() action
               uncomment below and update the code to test
                //instance.action(function(error) {
                //  if (error) throw error;
                //expect().to.be();
                //});
                done();
               */
})
